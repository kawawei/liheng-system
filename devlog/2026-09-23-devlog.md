# 開發日誌 2026-09-23

## 純映像檔架構轉型與伺服器非必要檔案清理 [15:30]

### 完成功能 / 修復內容
- [x] **落實本機原生算力 + 容器純裝配**：
    - 前端靜態資源由 Mac M 系列晶片原生全速執行 `pnpm --filter frontend build`（僅 10 秒）。
    - 前端 Dockerfile 改寫為純裝配模式，直接拷貝 `dist`，3 秒極速封裝成 `linux/amd64` 映像檔，完全杜絕 QEMU 模擬損耗。
- [x] **伺服器非必要檔案清理**：
    - 43 測試伺服器（43.103.3.57）成功移除原始碼（`backend/`, `frontend/`）、Git 庫（`.git/`）、依賴檔（`package.json`, `pnpm-lock.yaml`, `tsconfig.json` 等）與文檔。
    - 伺服器端轉為純映像檔極簡結構（僅存 `docker/`、`env/` 與 `deployment.log`）。
- [x] **修復 Nginx IPv6 健康檢查問題**：
    - 在 `nginx.conf` 增加 `listen [::]:80;` 並修正 healthcheck 端點為 `127.0.0.1`，使前端容器從 `unhealthy` 恢復為 `(healthy)`。
- [x] **全數容器健康運行驗證**：
    - 前後端、PostgreSQL、Redis 4 個容器皆處於 `Up (healthy)` 狀態，API 與靜態頁面 HTTP 200 回應正常。

### 專案結構 (Project Structure)
#### 本地專案結構
```text
liheng-system/
├── backend/                  # 後端 Node.js Express 模組
├── frontend/                 # 前端 React + Vite SPA 模組
├── docker/                   # Docker 配置目錄
│   ├── local/                # 本地開發 Compose
│   └── server/               # 伺服器純映像檔 Compose 與 Nginx 配置
├── env/                      # 環境變數配置目錄
├── devlog/                   # 開發日誌與 Issue 追蹤
└── .agent/workflows/         # 部署與作業流程
```

#### 43 伺服器端純映像檔結構
```text
/opt/project/liheng-system/
├── docker/
│   └── server/
│       ├── compose.yaml      # 純映像檔模式 (liheng-system-backend, frontend)
│       ├── nginx.conf        # Nginx 靜態代理配置 (支援 IPv4/IPv6)
│       └── .env -> ../../env/server/.env # 環境變數軟連結
├── env/
│   └── server/
│       └── .env              # 生產環境機密與連線設定
└── deployment.log            # 部署版本與遷移記錄
```

### 提交內容 (Commit: `6d3da34`)
- **提交時間**: 15:30
- **提交 Hash**: `6d3da34`
- **提交訊息**: `feat(deploy): migrate to image-based deployment and fix nginx healthcheck`
- **變更檔案**:
  - `docker/server/compose.yaml`：調整為純映像檔運行配置並修復健康檢查端點。
  - `docker/server/Dockerfile.frontend`：重構為純裝配模式。
  - `docker/server/nginx.conf`：增加 IPv6 監聽支援。
  - `.dockerignore`：增加放行 `frontend/dist` 規則。
  - `devlog/issue.md`：記錄 Nginx IPv6 與 .dockerignore 問題排查紀錄。
  - `devlog/2026-09-23-devlog.md`：記錄今日開發日誌。
- **變更統計**: 6 個檔案 (+91/-42)
